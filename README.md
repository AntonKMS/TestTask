# Test task

“естовое задание

## “ехнологии:

- Java версии >= 8 
- Spring Boot (standalone приложение)
- —”Ѕƒ PostgreSQL (верси€ не важна)
- Liquibase дл€ версионировани€ схемы Ѕƒ
- —борка проекта через Maven
-  онфигураци€ приложени€ через application.yml

## “ребовани€:

### —оздать 2 таблицы в Ѕƒ через Liquibase:
-	Car {id: Long PK,  driver_name: String,  brand: String,  garage_id: Long FK};
-	Garage: {id: Long PK, name: String}.
###	—оздать следующие методы REST API:
-	GET /api/garage Ц возвращает массив всех записей из таблицы garage;
-	GET /api/garage/{id} Ц возвращает объект Garage в виде JSON с вложенным списком машин в нем;
-	POST /api/garage Ц сохран€ет новый объекта Garage, передача названи€ в теле запроса в виде JSON;
-	GET /api/car Ц возвращает массив всех записей из таблицы car;
-	GET /api/car/{id} Ц возвращает объект Car в виде JSON;
-	POST /api/car Ц сохранение нового объекта Car, передача данных в теле запроса в виде JSON;
-	¬алидировать входные данные полей driver_name и brand на наличие только букв, иначе возвращать 400 BAD REQUEST с указанием проблемного пол€;
-	¬алидировать id гаража перед сохранением, если такого в базе нет, возвращать 400;
-	 „ерез конфигурационный параметр контролировать вместимость гаража, при попытке добавить новую машину в полный гараж возвращать 400 с описанием ошибки.