# Test task

# “ехнологии

##Х	Java версии >= 8 
##Х	Spring Boot (standalone приложение)
##Х	—”Ѕƒ PostgreSQL (верси€ не важна)
##Х	Liquibase дл€ версионировани€ схемы Ѕƒ
##Х	—борка проекта через Maven
##Х	 онфигураци€ приложени€ через application.yml

# “ребовани€:

1.	—оздать 2 таблицы в Ѕƒ через Liquibase:
	a.	Car {id: Long PK,  driver_name: String,  brand: String,  garage_id: Long FK}
	b.	Garage: {id: Long PK, name: String}
	2.	—оздать следующие методы REST API:
	a.	GET /api/garage Ц возвращает массив всех записей из таблицы garage 
	b.	GET /api/garage/{id} Ц возвращает объект Garage в виде JSON с вложенным списком машин в нем
	c.	POST /api/garage Ц сохран€ет новый объекта Garage, передача названи€ в теле запроса в виде JSON
	d.	GET /api/car Ц возвращает массив всех записей из таблицы car 
	e.	GET /api/car/{id} Ц возвращает объект Car в виде JSON 
	f.	POST /api/car Ц сохранение нового объекта Car, передача данных в теле запроса в виде JSON
	i.	¬алидировать входные данные полей driver_name и brand на наличие только букв, иначе возвращать 400 BAD REQUEST с указанием проблемного пол€
		ii.	¬алидировать id гаража перед сохранением, если такого в базе нет, возвращать 400
			iii.	 „ерез конфигурационный параметр контролировать вместимость гаража, при попытке добавить новую машину в полный гараж возвращать 400 с описанием ошибки
